## VIM reading a file
:r ./flag.txt

## SSHing and getting access using VIM
If SSH connection is closing and a big text is being displayed, make the window smaller to make it need to go into VIM.
Then if the default shell is /bin/bash use `:shell`. If not, set the shell first to `:set shell=/bin/bash`, then use `:shell`.
